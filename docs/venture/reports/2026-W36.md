Week 2026-08-31 → 09-06 · OnCall fork (codename Mansoor)
metric_now: Grafana stable releases the fork is verified on · 13.2.1 by browser check and build; e2e in CI pending (run after L-10) · target: e2e green on the current Grafana stable within 14 days of each minor release

B-01 "the fork keeps working on every current Grafana release, proven by CI"
  DONE   L-02  The plugin no longer crashes on the current Grafana, so paging keeps working after an upgrade.          risk       4397c8f4..751f49e3
  DONE   L-03  The Grafana container stops restarting under load on the hobby and developer stacks.                  risk       ee2a75bb
  DONE   L-04  Every push runs the full test suite on GitHub and shows the result on the README badge.               velocity   5f286a19
  DONE   L-06  A release is one tag push; the image and the plugin archive appear without manual steps.              velocity   run 33781194386
  DONE   L-07  Users can install a version of OnCall that works on Grafana 13 instead of the archived build.          retention  v1.17.0
  DONE   L-08  A fresh install pulls the fixed plugin and engine, not the archived ones that crash.                   retention  bec1974a
  DONE   L-10  The e2e database starts again; Helm users stop depending on a tag that changes under them.            risk       f4d4abe1
B-02 "phone calls and SMS through Twilio work end to end"
  AWAITING T-06  A real call needs a Twilio account, a number and credentials from the operator.                     retention
B-03 "the engine runs on a supported stack"
  (no rows yet)
Ongoing: T-01 (risk, tests.yml run after the MariaDB pin)
Awaiting you: 12 proposals in backlog.md ## Proposed · Twilio credentials for T-06
Not done: e2e not yet green on dev; nothing merged beyond dev (the fork has no main)
